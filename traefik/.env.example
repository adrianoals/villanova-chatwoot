# =============================================================================
# Traefik — production .env template
# =============================================================================
# Atualmente o docker-compose.yaml não usa nenhuma variável obrigatória,
# mas este arquivo existe pra futuras configurações (ex.: email Let's Encrypt
# parametrizado, basic-auth do dashboard, etc).
#
# O email atual do Let's Encrypt está hardcoded no docker-compose.yaml
# (campo --certificatesresolvers.le.acme.email). Pra trocar, edite lá.
